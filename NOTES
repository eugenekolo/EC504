# Some notes to developers

`brew install gradle` will install gradle on OSX.
`gradle wrapper` will install the wrapper. Perhaps not necessary.
Run `./build.sh` to build the project.
Run `./run.sh` to run the project.

